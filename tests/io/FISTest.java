import org.bzdev.io.*;
import java.io.*;

public class FISTest {

    public static void main(String argv[]) throws Exception {
	/*
	if (FISOutputStream.isSupported() == false) {
	    System.out.println("FIS not supported");
	    System.exit(0);
	}


	OutputStream os = new FileOutputStream("fis.out");
	FISOutputStream fos = new FISOutputStream(os);
	String text[] = {"<?xml version=\"1.0\"?>",
			 "<note>",
			 "<date>2015-09-01</date>",
			 "<hour>08:30</hour>",
			 "    ",
			 "<to>Tove</to>",
			 "<to>Dave</to>",
			 "<to>Jeff</to>",
			 "<to>Jeff</to>",
			 "<from>Jani</from>",
			 "<body>Don't forget me this weekend!</body>",
			 "</note>"};

	PrintStream ps = new PrintStream(fos, true, "UTF-8");

	for (String line: text) {
	    ps.println(line);
	}
	fos.flush();
	fos.close();
	*/
    }
 }
