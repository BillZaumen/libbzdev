
global.generateDocs(w, ["/usr/share/doc/libbzdev-doc/api",
                        "/usr/share/doc/openjdk-11-doc/api"]);

var out = global.getWriter();

IntStream.range(0, 1000)
    .peek(out::println)
    .mapToObj(String::valueOf)
    .peek(function(x) {out.println(global.typeof(x)); out.println(x); void})
    .findFirst()
    .ifPresent(function(x) {out.println(x); void});

/*
    .peek(out::println)


IntStream.range(0, 1000)
    .peek(out::println)
    .mapToObj(String::valueOf)
    .peek(out::println)
    .findFirst()
    .ifPresent(out::println);

var result = 
IntStream.range(0, 1000)
    .peek(function(x) {out.println(x); void})
    .mapToObj(String::valueOf)
    .peek(function(x) {out.println(x); void})
    .findFirst();
out.println(result);
var pres = result.isPresent();
out.println(pres);
result.ifPresent(function(x) {out.println(x); void});
*/
out.flush();
